apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  creationTImestamp: null
  labels:
    io.kompose.service: db
  name: db
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
